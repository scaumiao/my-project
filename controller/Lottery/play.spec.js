var play = require('./play');
var expect = require('chai').expect;
var keystone = require('keystone');

describe('check lottery', function() {
  //测试方法是否存在
  it("should return the true count", function() {
    // expect(play.get_list()).to.eql(6);
  });
});
